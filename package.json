{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.15.0"
  },
  "scripts": {
    "vue-examples:serve": "yarn --cwd \"packages/vue-examples\" serve",
    "core:prepare": "yarn --cwd \"packages/core\" prepare"
  }
}
